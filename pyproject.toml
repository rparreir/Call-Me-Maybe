[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ruben", email = "rubenparreira11@gmail.com" } #mudar este email para o da escola
]
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pydantic",
    "llm_sdk"
]

[tool.uv.sources]
llm_sdk = { path = "./llm_sdk", editable = true }